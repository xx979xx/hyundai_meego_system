<?xml version="1.0"?>
<actions>

  <tracker-condition name="contact-with-phone-number">
    { ?uri a nco:Contact ;
           nco:hasPhoneNumber ?phone . }
  </tracker-condition>

  <tracker-condition name="image">
    { ?uri a nfo:Image . }
  </tracker-condition>

  <tracker-condition name="software-application">
    { ?uri a nfo:Software . }
  </tracker-condition>

  <tracker-condition name="calendar-event">
    { ?uri a ncal:Event . }
  </tracker-condition>

  <tracker-condition name="calendar-journal">
    { ?uri a ncal:Journal . }
  </tracker-condition>

  <tracker-condition name="calendar-todo">
    { ?uri a ncal:Todo . }
  </tracker-condition>

  <tracker-condition name="call">
    { ?uri a nmo:Call . }
  </tracker-condition>

  <tracker-condition name="person-contact">
    { ?uri a nco:PersonContact . }
  </tracker-condition>

  <tracker-condition name="email">
    { ?uri a nmo:Email . }
  </tracker-condition>

  <tracker-condition name="paginated-text-document">
    { ?uri a nfo:PaginatedTextDocument . }
  </tracker-condition>

  <tracker-condition name="plaintext-document">
    { ?uri a nfo:PlainTextDocument . }
  </tracker-condition>

  <tracker-condition name="presentation">
    { ?uri a nfo:Presentation . }
  </tracker-condition>

  <tracker-condition name="spreadsheet">
    { ?uri a nfo:Spreadsheet . }
  </tracker-condition>

  <tracker-condition name="email">
    { ?uri a nmo:Email . }
  </tracker-condition>

  <tracker-condition name="feed-message">
    { ?uri a mfo:FeedMessage . }
  </tracker-condition>

  <tracker-condition name="folder">
    { ?uri a nfo:Folder . }
  </tracker-condition>

  <tracker-condition name="im-message">
    { ?uri a nmo:IMMessage . }
  </tracker-condition>

  <tracker-condition name="music-piece"> 
    { ?uri a nmm:MusicPiece . }
  </tracker-condition>

  <tracker-condition name="phone-number">
    { ?uri a nco:PhoneNumber . }
  </tracker-condition>

  <tracker-condition name="photo">
    { ?uri a nmm:Photo . }
  </tracker-condition>

  <tracker-condition name="radio-station">
    { ?uri a nmm:RadioStation . }
  </tracker-condition>

  <tracker-condition name="sms-message">
    { ?uri a nmo:SMSMessage . }
  </tracker-condition>

  <tracker-condition name="tag">
    { ?uri a nao:Tag . }
  </tracker-condition>

  <tracker-condition name="video">
    { ?uri a nfo:Video . }
  </tracker-condition>

  <tracker-condition name="device-captured-video">
    { ?uri a nfo:Video ;
    nfo:equipment "urn:equipment:booti:008:" . }
  </tracker-condition>

  <tracker-condition name="web-history">
    { ?uri a nfo:WebHistory . }
  </tracker-condition>

  <tracker-condition name="mms-message">
    { ?uri a nmo:MMSMessage . }
  </tracker-condition>

</actions>
